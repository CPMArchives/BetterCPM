# BetterCP/M 1.0 Roadmap

## Status and authority

This is the canonical ordering of the remaining BetterCP/M 1.0 work. The
project backlog records individual tasks, architecture documents define
accepted contracts, and engineering specifications record implementation and
qualification evidence. None of those lists may silently reorder this roadmap
or collapse an architecture stage into its later implementation work.

Stages 1 and 2 established the project-wide ABI design conventions and the
resident-service ABI/registry. Stage 3 is the current critical path. This
revision deliberately narrows the remaining design stages and moves HISTORY
service redesign, named directories/PATH, a general dynamic PDS allocator, and
broad historical date/time compatibility to later 1.x work. A usable first
release is the goal; the backlog is not the release contract.

## Architectural critical path

### Stage 3 — Complete stateful RSX reconstruction

Carrier loading and normalization are implemented. Finish the Function 202
transaction coordinator by connecting the validated carrier to prospective
layout planning, stateful-pointer preparation, snapshot construction,
movement, and the disk-free commit. Select that path from production Function
202, complete persistent-profile publication and resolver restoration, and
qualify the complete transaction on both 1.0 platforms. Forced relocation must
preserve declared mutable state and internal pointers; a failed transaction
must leave the live profile unchanged. Reconstruction, movement, publication
and commit must modify only RAM-owned state, never executable code or immutable
descriptors. This rule does not require Stage 3 to complete the later ROM/RAM
relocation.

The prepared append/removal pipeline now supplies production Function 202 and
reaches commit, persistent-profile publication, resolver restoration, and
resident self-retirement (engineering specification 151). The legacy manager,
validator, publisher, and version-1 carrier path have been removed. The complete
pipeline has passed cross-platform stateful relocation, rollback, warm-boot and
immutable-code qualification. Stage 3 is complete.

### Final 1.0 boundary — frozen

No Stage 3 implementation finding invalidated this reduced release boundary:

- trs80gp/Model 4 and z80pack/cpmsim are the two 1.0 targets;
- the default configuration retains at least 53 KiB usable TPA;
- the 1.0 ROM profile executes immutable code in place under enforced write
  protection in the selected z80pack/cpmsim qualification environment, with all
  mutable state in RAM;
- terminal-capability redesign is post-1.0;
- a bounded single-bank RomWBW/HBIOS port is a 1.1 or 1.2 candidate; and
- generalized bank-aware firmware and full RomWBW integration are 2.0 work;
- the existing bounded persistent command history is sufficient for 1.0;
  `HISTORY.RSX` redesign is later 1.x work;
- canonical `DU:` operation is sufficient for 1.0; named directories, PATH,
  NDR management, and their command integration are later 1.x work;
- the 1.0 PDS needs a versioned, measured inventory for actual 1.0 owners, but
  general allocation, migration, and controlled runtime expansion are later
  1.x capabilities;
- writable resident objects are classified as bounded PDS state, fixed
  subsystem state, stacks or temporary/shared workspace before placement;
  temporary storage may be statically overlaid only after proving mutually
  exclusive lifetimes;
- mutable objects initialized from constants use immutable ROM templates and
  RAM live instances; E300h remains a planning boundary rather than ABI;
- FDF/FDB, normalized disk state, CONFIG, DUP, SYSGEN, and installation remain
  1.0 requirements;
- 1.0 retains the callable `TIME` ABI as its native clock service, keeps
  `TIME.COM` on that service, and qualifies read-only FreHD and z80pack
  providers; the separate `P2DOS.RSX` frontend supplies Functions 200/201 without
  duplicating hardware code; separate four-byte and five-byte BDOS 104/105
  frontends are bounded optional candidates only after `P2DOS.RSX` is complete
  and measured, while writable providers, other historical clock APIs, and
  filesystem timestamps are later 1.x work; and
- the 1.0 utility inventory remains deliberately small and is selected only
  after the retained system facilities are complete; the backlog list is
  candidate inventory and suitably licensed existing utilities may satisfy the
  final selection; and
- 1.0 preserves and permits inspection or change of the ordinary read-only,
  system and archive attribute bits, enforces read-only, treats archive as
  metadata only, and defers enhanced system-file visibility across user areas.

### Stage 4 — Normalize disk state

Architecture Specification 28 defines the authoritative model for physical
records, self-contained normalized logical bindings, saved cold-boot state and
observed/derived state. The BIOS-owned records are operational authority; FDB
identity is provenance. Validated synchronous transitions, explicit dependency
invalidation, managed capability removal, media-state separation, DUP/SYSGEN
requirements and CONFIG H's verified but not power-failure-atomic persistence
contract are frozen. The source audit is recorded in
`docs/engineering/STAGE-4-DISK-STATE-SOURCE-AUDIT.md`. Stage 4 is complete.

### Stage 5 — Define the bounded 1.0 PDS

Inventory the resident-service registry, reconstruction records, existing
history state, normalized disk state, configuration state, and every other
accepted 1.0 owner. Define discovery, ownership, versioning, initialization,
reset behavior, fixed sizing, and failure behavior while preserving the 53 KiB
TPA floor. Do not design a general heap or runtime-expansion mechanism for 1.0.

### Stage 6 — Freeze extension and clock interfaces

Freeze the CPX, RSX, resident-service, and private BDOS namespaces. Before the
ABI is declared stable, reassign BetterCP/M CPX control away from historical
P2DOS Function 200 and remove the nonproduction HELLO and ECHO proof services
from the released namespace; they currently use Functions 201 and 203 solely
to exercise RSX chaining. Rebuild every in-tree client and implement 200/201 as
the inherited P2DOS-compatible get/set clock calls. The completed inventory is
normative in Architecture Specification 25: 176-196 are production services,
197 is an undefined reserved future escape, and 198-199 are non-public tests.
Stage 6 migrates all provisional private calls to the registered 176-183
assignments and gives unhandled 200/201 calls the mechanical 0FFh fallback.
It freezes the existing callable `TIME` ABI as the native clock service,
migrates its registry lookup from provisional Function 208 to Function 182,
and keeps `TIME.COM`, `FREHDCLK.RSX`, and `ZPRTC.RSX` on that native boundary.
Test-only selectors remain outside the frozen application ABI.

After `P2DOS.RSX` is complete and qualified, measure the incremental cost of
two mutually exclusive BDOS 104/105 frontends. The four-byte profile implements
the CP/M 3/DOS+ 2.5 record and returns seconds in A; the five-byte profile
implements the Z80DOS 2.4 record and returns A=00h. They are optional 1.0
candidates, not release gates. They may be included only if they remain small,
stateless adapters over TIME and require no Function-12 impersonation, new
lifecycle architecture, filesystem timestamps, or other originating-system
behavior. No frontend may infer a caller's buffer convention heuristically.

A separate optional `P2DOS.RSX` frontend intercepts Functions 200/201 and consumes
the native `TIME` service; it contains no hardware driver. Freeze exact P2DOS
success behavior and the minimal public status set, then qualify provider and
frontend replacement, unload, and WBOOT. Both required 1.0 providers may
remain read-only; Function 201 maps native SET_UNSUPPORTED through the
frontend. Other historical frontends may later consume the same native
service without modifying or duplicating a hardware provider.

### Stage 7 — Publish retained implementation contracts

Turn the reduced boundary into implementation-ready acceptance specifications
for the core and ROM profile, command and batch environment, disk-format and
installation path, required CP/M file attributes, native clock facility, and
release tooling. Reject any design that implicitly restores a deferred feature
as a prerequisite. The ROM profile follows Architecture Specification 27:
ownership precedes placement, lifetime precedes allocation, and mutable live
objects are instantiated in RAM from immutable templates.

### Stage 8 — Reconcile and publish the architecture

Reconcile architecture documents, engineering specifications, programmer
guides, the backlog, and the approved reduced 1.0 scope. Resolve obsolete or
contradictory text and publish implementation-ready specifications for Stages
4 through 7. Mark deferred work explicitly instead of deleting its design
history. This is an architecture gate before broad implementation; it is
separate from final user manuals, release notes, and packaging documentation.
Chat should perform the bulk documentation reconciliation to conserve Codex
credits for engineering and implementation. Codex remains responsible for
source-derived technical verification, repository-specific corrections, and
any executable or generated evidence needed to make the reconciled text
authoritative.

## Implementation program

Implementation begins after Stage 8 and follows the accepted specifications:

1. Finish and qualify the core, PDS implementation, and 1.0 ROM profile.
2. Finalize CCP, CPX, RSX, command-input, and required SUBMIT/XSUB behavior
   using the existing bounded history facility.
3. Implement FDF/FDB against the normalized disk-state model.
4. Complete CONFIG, DUP, SYSGEN, installation, and platform work.
5. Complete CP/M-required attribute behavior; qualify the native `TIME` ABI,
   `TIME.COM` GET/SET handling, and the FreHD and z80pack providers; and qualify
   the separate `P2DOS.RSX` Functions 200/201 frontend. Then measure and, only
   if still trivial and schedule-neutral, implement the optional four-byte and
   five-byte BDOS 104/105 frontends. Do not add filesystem timestamps or treat
   these clock-call profiles as broader historical operating-system support.
6. As the final design decision, define the accompanying 1.0 utility and disk
   inventory.
7. Implement or adopt the selected utilities, assemble the distribution, and
   complete release documentation.
8. Freeze the release-candidate images and run the complete conformance suite
   against those exact images on both 1.0 targets: trs80gp/Model 4 and
   z80pack/cpmsim. Archive the commands, suite version, configuration, and
   results as release evidence; resolve every unexplained regression before
   completing the remaining release qualification and declaring 1.0 ready.

The utility inventory deliberately remains the final design decision. Further
machine surveys, HISTORY.RSX, named directories/PATH, general dynamic PDS
allocation, historical date/time compatibility beyond the bounded optional
104/105 clock-call profiles, filesystem timestamps, the
2.0 bank architecture, terminal-capability redesign, and the future
mixed-track FDF/FDB extension remain at their assigned post-1.0 decision
points unless a required 1.0 target demonstrates a blocking need.

## Later 1.x candidates

The deferred features remain planned work rather than abandoned ideas. Suitable
1.x increments include:

- `HISTORY.RSX` and its state/lifecycle contract;
- named directories, PATH semantics, NDR management, and command integration;
- a general PDS allocator, migration, and controlled runtime expansion;
- writable clock-provider implementation and qualification beyond the 1.0
  read-only platform providers; Function 201 and TIME.COM's SET request already
  belong to the 1.0 interface;
- any BDOS 104/105 frontend not admitted by the bounded optional 1.0 test, plus
  DateStamper and ZSDOS/ZDDOS adapters and broader P2DOS, CP/M Plus, DOS+, and
  Z80DOS compatibility where practical;
- filesystem timestamp layouts and host-tool/cpmtools interoperability as a
  separate feature from clock access; and
- the Developer Platform workstream defined by Architecture Specification 26:
  supported introspection, third-party identity policy, optional hooks and
  diagnostics, an extension SDK, native development tools, and native
  build/test integration; and
- the bounded single-bank RomWBW/HBIOS port and selected additional validation
  platforms.

No particular minor release is promised for all of these items. Each can ship
when its own ABI, memory cost, compatibility boundary, and qualification are
complete.

## Continuous acceptance gates

Every implementation increment must preserve the accepted ABI boundaries,
remain buildable for both 1.0 targets, and retain at least 53 KiB usable TPA in
the default configuration. ROM-profile work must ultimately pass actual
execution-in-place testing with enforced write protection. Compatibility and
memory evidence are collected continuously rather than deferred until release
assembly.

When freezing or publishing an interface during Stages 3 through 8, review it
against Architecture Specification 26. Preserve future developer-facing
headroom when an ABI-neutral adjustment has negligible 1.0 cost. Do not add
speculative hooks, resident facilities, selectors, dispatch layers or
compatibility promises to satisfy a possible future use.
